# Generated by bun run openapi:generate. Do not edit by hand.
openapi: 3.1.0
info:
  title: hybrd API
  version: 0.1.0
  description: Local-first training control plane. Operations marked scaffold return 501 and do not
    persist or acknowledge work. Better Auth endpoints live under /api/auth and use the provider's
    contract.
servers:
  - url: http://localhost:3000
    description: Local development; override in the generated client for other environments.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Better Auth session token. Store it in the iOS Keychain.
  schemas:
    ApiError:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: object
              additionalProperties: {}
            requestId:
              type: string
              format: uuid
          required:
            - code
            - message
            - requestId
      required:
        - error
    Bootstrap:
      type: object
      properties:
        athlete:
          $ref: "#/components/schemas/Athlete"
        device:
          type: object
          properties:
            registered:
              type: boolean
          required:
            - registered
        sync:
          type: object
          properties:
            available:
              type: boolean
            latestSequence:
              $ref: "#/components/schemas/SyncCursor"
          required:
            - available
            - latestSequence
        policy:
          type:
            - object
            - "null"
          properties:
            version:
              type: integer
            checksum:
              type: string
          required:
            - version
            - checksum
        entitlements:
          type: array
          items:
            $ref: "#/components/schemas/Entitlement"
      required:
        - athlete
        - device
        - sync
        - policy
        - entitlements
    Athlete:
      type: object
      properties:
        id:
          type: string
          format: uuid
        timezone:
          type: string
        locale:
          type: string
        distanceUnit:
          type: string
          enum:
            - km
            - mi
        loadUnit:
          type: string
          enum:
            - kg
            - lb
        weekStartsOn:
          type: integer
          minimum: 1
          maximum: 7
        revision:
          $ref: "#/components/schemas/SyncCursor"
      required:
        - id
        - timezone
        - locale
        - distanceUnit
        - loadUnit
        - weekStartsOn
        - revision
    SyncCursor:
      type: string
      maxLength: 19
      pattern: ^(0|[1-9][0-9]*)$
      example: "0"
    Entitlement:
      type: object
      properties:
        key:
          type: string
        status:
          type: string
          enum:
            - active
            - grace
            - expired
            - revoked
        validUntil:
          type:
            - string
            - "null"
          format: date-time
      required:
        - key
        - status
        - validUntil
    TrainingPolicy:
      type: object
      properties:
        version:
          type: integer
          exclusiveMinimum: 0
        schemaVersion:
          type: integer
          exclusiveMinimum: 0
        checksum:
          type: string
        config:
          type: object
          additionalProperties: {}
        minimumAppVersion:
          type:
            - string
            - "null"
      required:
        - version
        - schemaVersion
        - checksum
        - config
        - minimumAppVersion
    RegisteredDevice:
      type: object
      properties:
        id:
          type: string
          format: uuid
        registered:
          type: boolean
          enum:
            - true
      required:
        - id
        - registered
    DeviceRegistration:
      type: object
      properties:
        appVersion:
          type: string
          minLength: 1
          maxLength: 64
        osVersion:
          type: string
          minLength: 1
          maxLength: 64
        pushToken:
          type:
            - string
            - "null"
          minLength: 32
          maxLength: 512
          pattern: ^[a-fA-F0-9]+$
        pushEnabled:
          type: boolean
          default: false
      required:
        - appVersion
      additionalProperties: false
    SyncPush:
      type: object
      properties:
        deviceId:
          type: string
          format: uuid
        mutations:
          type: array
          items:
            $ref: "#/components/schemas/SyncMutation"
          minItems: 1
          maxItems: 100
      required:
        - deviceId
        - mutations
      additionalProperties: false
    SyncMutation:
      type: object
      properties:
        id:
          type: string
          format: uuid
        entityType:
          type: string
          minLength: 1
          maxLength: 64
        entityId:
          type: string
          format: uuid
        operation:
          type: string
          enum:
            - create
            - update
            - delete
            - activate_plan
        baseRevision:
          allOf:
            - $ref: "#/components/schemas/SyncCursor"
            - type:
                - string
                - "null"
        payload:
          type: object
          additionalProperties: {}
      required:
        - id
        - entityType
        - entityId
        - operation
        - baseRevision
        - payload
      additionalProperties: false
    DecisionRequest:
      type: object
      properties:
        decisionType:
          type: string
          enum:
            - next_week_running_load
        schemaVersion:
          type: number
          enum:
            - 1
        context:
          $ref: "#/components/schemas/RunningLoadContext"
      required:
        - decisionType
        - schemaVersion
        - context
      additionalProperties: false
    RunningLoadContext:
      type: object
      properties:
        completionRate:
          type: number
          minimum: 0
          maximum: 1
        weeklyDistanceKm:
          type: number
          minimum: 0
        previousWeeklyDistanceKm:
          type: number
          minimum: 0
        keyRunCompletionRate:
          type: number
          minimum: 0
          maximum: 1
        easyRunRpeTrend:
          type:
            - number
            - "null"
        missedSessionsLast14d:
          type: integer
          minimum: 0
        strengthPerformanceTrend:
          type:
            - number
            - "null"
        raceWeeksRemaining:
          type:
            - integer
            - "null"
          minimum: 0
      required:
        - completionRate
        - weeklyDistanceKm
        - previousWeeklyDistanceKm
        - keyRunCompletionRate
        - easyRunRpeTrend
        - missedSessionsLast14d
        - strengthPerformanceTrend
        - raceWeeksRemaining
      additionalProperties: false
    CoachRequest:
      type: object
      properties:
        threadId:
          type: string
          format: uuid
        message:
          type: string
          minLength: 1
          maxLength: 8000
        context:
          type: object
          properties:
            schemaVersion:
              type: number
              enum:
                - 1
            activePlanVersionId:
              type:
                - string
                - "null"
              format: uuid
            summary:
              type: string
              maxLength: 12000
          required:
            - schemaVersion
            - activePlanVersionId
            - summary
          additionalProperties: false
      required:
        - threadId
        - message
        - context
      additionalProperties: false
    AppleTransactionSubmission:
      type: object
      properties:
        signedTransaction:
          type: string
          minLength: 1
          maxLength: 32000
      required:
        - signedTransaction
      additionalProperties: false
  parameters: {}
paths:
  /health/live:
    get:
      operationId: getLiveness
      tags:
        - Health
      responses:
        "200":
          description: Process is running.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - ok
                required:
                  - status
  /health/ready:
    get:
      operationId: getReadiness
      tags:
        - Health
      responses:
        "200":
          description: Database is reachable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - ready
                required:
                  - status
        "503":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/bootstrap:
    get:
      operationId: getBootstrap
      tags:
        - Athlete
      security: &a1
        - bearerAuth: []
      parameters:
        - schema:
            type: string
            format: uuid
          required: false
          name: deviceId
          in: query
      responses:
        "200":
          description: Account state and capabilities. Sync is unavailable in this scaffold.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Bootstrap"
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/config/training-policy:
    get:
      operationId: getTrainingPolicy
      tags:
        - Configuration
      security: *a1
      parameters:
        - schema:
            type: string
          required: false
          name: if-none-match
          in: header
      responses:
        "200":
          description: Latest published policy. Cache only if schemaVersion is supported.
          headers:
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrainingPolicy"
        "304":
          description: Cached policy is current.
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/devices/{deviceId}:
    put:
      operationId: registerDevice
      tags:
        - Devices
      security: *a1
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: deviceId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeviceRegistration"
      responses:
        "200":
          description: Idempotent registration of the authenticated athlete's installation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisteredDevice"
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: revokeDevice
      tags:
        - Devices
      security: *a1
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: deviceId
          in: path
      responses:
        "204":
          description: Installation revoked. Repeated requests have the same effect.
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/billing/entitlements:
    get:
      operationId: getEntitlements
      tags:
        - Billing
      security: *a1
      responses:
        "200":
          description: Canonical entitlement records; an empty array grants no access.
          content:
            application/json:
              schema:
                type: object
                properties:
                  entitlements:
                    type: array
                    items:
                      $ref: "#/components/schemas/Entitlement"
                required:
                  - entitlements
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/sync/push:
    post:
      operationId: pushSync
      tags:
        - Sync
      security: *a1
      summary: "Scaffold: no mutations are accepted or acknowledged yet."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SyncPush"
      responses:
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "501":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/sync/pull:
    get:
      operationId: pullSync
      tags:
        - Sync
      security: *a1
      summary: "Scaffold: no change feed is served yet."
      parameters:
        - schema:
            $ref: "#/components/schemas/SyncCursor"
          required: false
          name: cursor
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
          required: false
          name: limit
          in: query
      responses:
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "501":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/intelligence/decision:
    post:
      operationId: requestDecision
      tags:
        - Intelligence
      security: *a1
      summary: "Scaffold: structured decision gateway. No provider is called."
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: idempotency-key
          in: header
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecisionRequest"
      responses:
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "501":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/intelligence/chat:
    post:
      operationId: sendCoachMessage
      tags:
        - Intelligence
      security: *a1
      summary: "Scaffold: coach gateway. Streaming and proposal contracts are not implemented."
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: idempotency-key
          in: header
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CoachRequest"
      responses:
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "501":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/billing/apple/transactions:
    post:
      operationId: submitAppleTransaction
      tags:
        - Billing
      security: *a1
      summary: "Scaffold: signed transaction verification. No entitlement is granted."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AppleTransactionSubmission"
      responses:
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "501":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /v1/account:
    delete:
      operationId: deleteAccount
      tags:
        - Account
      security: *a1
      summary: "Scaffold: account deletion is pending a retention policy."
      responses:
        "400":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "501":
          description: Request failed. Use the stable error code for client behavior.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
webhooks: {}
